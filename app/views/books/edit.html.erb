<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>
<div class= "a">
 <div class= "b">
   Title
   <div class= "c">
    <%= f.text_field :title %>
   </div>
  </div>

  <div class= "b">Body
   <div class= "c">
    <%= f.text_area :body %>
   </div>
  </div>

  <%= f.submit 'Update Book' %>
  </div>

<% end %>

<%= link_to "Show", book_path(@book.id) %>
<%= "|"%>
<%= link_to "Back", books_path %>